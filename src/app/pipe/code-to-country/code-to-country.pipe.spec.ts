import { CodeToCountryPipe } from './code-to-country.pipe';

describe('CodeToCountryPipe', () => {
  it('create an instance', () => {
    const pipe = new CodeToCountryPipe();
    expect(pipe).toBeTruthy();
  });
});
