import { ClientNowPipe } from './client-now.pipe';

describe('ClientNowPipe', () => {
  it('create an instance', () => {
    const pipe = new ClientNowPipe();
    expect(pipe).toBeTruthy();
  });
});
